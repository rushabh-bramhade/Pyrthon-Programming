import numpy as np

arr = np.zeros([
    [a],[b],
    
])

#for inserting 

for i in range(arr.shape[0]):
    for j in range(arr.shape[1]):
        print(int(input("Enter the number :",arr[i][j])))
    
print(arr)
        